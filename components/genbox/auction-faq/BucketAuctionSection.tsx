import { Flex } from "@chakra-ui/react";

function BucketAuctionSection() {
  return (
    <Flex
      direction="column"
      w={["full", "full", "full", "60%"]}
      position="relative"
      alignItems="center"
    >
    </Flex>
  );
}

export default BucketAuctionSection;
